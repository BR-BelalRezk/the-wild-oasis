import PageSection from "@/components/ui/page-section";
import UpdateSettings from "../content";

export default function SettingsPage() {
  return (
    <PageSection id="settings" heading="Update Settings">
      <UpdateSettings />
    </PageSection>
  );
}
